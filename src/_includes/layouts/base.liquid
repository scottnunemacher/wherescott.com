<!DOCTYPE html>
<html class="h-100" lang="{{ metadata.language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="{{ metadata.description }}" />
    <meta name="author" content="{{ metadata.author.name }}" />
    <title>{{ title }}</title>
    <!-- Favicon-->
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/images/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/images/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/images/favicon/favicon-16x16.png">
    <link rel="manifest" href="/assets/images/favicon/site.webmanifest">
    <!-- Google font-->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@100;200;300;400;500;600;700;800;900&amp;display=swap" rel="stylesheet" />
    <!-- Bootstrap icons-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet" />
    <!-- Bootstrap Based Theme CSS-->
    <link href="/assets/css/style.css" rel="stylesheet" />
    <!--<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">-->
</head>
<body class="d-flex flex-column bg-white h-100">
    <!-- Header-->
    <header>
        <div class="container">
            <nav class="d-flex flex-wrap align-items-center justify-content-center py-3 border-bottom">
                <a href="/" class="d-flex align-items-center mb-lg-0 me-sm-auto text-dark text-decoration-none">
                    <span class="fw-bolder fs-5">{{ metadata.emoji }} {{ metadata.title }}</span>
                </a>{% assign navPages = collections.all | eleventyNavigation %}
                <ul class="nav nav-pills mb-lg-0 fw-bolder">
                    {%- for entry in navPages %} 
                    <li class="nav-item"><a class="nav-link" href="{{ entry.url }}">{{ entry.title }}</a></li>
                    {%- endfor %}
                </ul>
            </nav>
        </div>
    </header>
    <!-- Main-->
    <main class="flex-grow-1">
        <div class="container px-4 py-5 h-100">
            {{ content }}
        </div>
    </main>
    <!-- Footer-->
    <footer class="mt-auto">
        <div class="container">
            <div class="d-flex flex-column-reverse flex-sm-row align-items-center justify-content-center py-3 border-top">
                <div class="d-flex align-items-center me-sm-auto text-dark text-decoration-none">
                    {{ metadata.copyright }}
                </div>
                <ul class="nav nav-pills mb-2 mb-sm-0 text-decoration-none align-items-center justify-content-center">
                    <a href="/">Privacy</a>
                    <span class="mx-1">&middot;</span>
                    <a href="/">Contact</a>
                </ul>
            </div>
        </div>
    </footer>
    <!-- Bootstrap JS-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>
